from django.conf.urls import url

# from .views import CouponListApi

app_name = 'coupon'
urlpatterns = [
    # url(r'^coupon/$', CouponListApi.as_view(), name='couponList'),
    # url(r'^coupon/(?P<pk>[0-9]+)/$', CouponListApi.as_view(), name='couponList'),
]